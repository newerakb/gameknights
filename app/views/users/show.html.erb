<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= "#{@user.username} (#{@user.email})" %>
      </h1>
      <%= link_to "Create a team", "#", class: "btn btn-large btn-primary" %>
    </section>
  </aside>
</div>
